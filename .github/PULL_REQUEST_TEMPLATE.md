## What this PR does

(One paragraph. Skip the narrative — say what changed.)

## Why

(Link to issue, or explain the motivation.)

## Testing

- [ ] `pytest -q` passes locally
- [ ] `ruff check .` passes
- [ ] `mypy src` passes
- [ ] If user-visible: added line to `CHANGELOG.md` under `[Unreleased]`

## Notes for reviewers

(Anything subtle the reviewer should know — e.g. why this looks weird, what you considered and rejected.)
